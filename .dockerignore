# Version control and local tooling
.git
.gitignore
.github
.vscode
.idea

# Python caches and local virtual environments
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Secrets and environment files
.env
.env.*
!.env.example
!*.env.example
**/.env
**/.env.*
!**/.env.example
!**/*.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*

# Local runtime data, logs, backups and databases
*.log
logs/
runtime/
data/
backups/
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm

# VPS host-only recovery/rollback working material must never enter an image.
# These paths can contain multi-gigabyte restore copies that are intentionally
# kept on the host but have no place in the immutable application image.
deploy/vps/backups/
deploy/vps/emergency-recovery/
deploy/vps/docker-compose.yml.bak-*
deploy/vps/*.bak-*

# OS and editor noise
.DS_Store
Thumbs.db

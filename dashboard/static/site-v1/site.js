const $=s=>document.querySelector(s),m=$('#message');document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-mode]').forEach(x=>x.classList.toggle('active',x===b));$('#loginForm').hidden=b.dataset.mode!=='login';$('#registerForm').hidden=b.dataset.mode!=='register';m.textContent=''});async function post(url,data){let r=await fetch(url,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),j=await r.json().catch(()=>({}));if(!r.ok)throw j;return j}$('#loginForm').onsubmit=async e=>{e.preventDefault();try{await post('/api/auth/login',{...Object.fromEntries(new FormData(e.target)),display_name:''});location='/app'}catch{m.textContent='Не удалось войти. Проверьте данные или дождитесь одобрения.'}};$('#registerForm').onsubmit=async e=>{e.preventDefault();let v=Object.fromEntries(new FormData(e.target));if(v.password!==v.password_confirmation){m.textContent='Пароли не совпадают.';return}try{let r=await post('/api/site-v1/access-requests',v);e.target.reset();m.textContent=r.message;m.className='ok'}catch(e){m.textContent=e?.detail?.status==='already_exists'?'Такой e-mail уже зарегистрирован.':'Проверьте поля и повторите попытку.'}};

name: Scratch Cancel Tests PR193

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: tests-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cancel-obsolete-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete PR193 Tests run cancelled by concurrency replacement."

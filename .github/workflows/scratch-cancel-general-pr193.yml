name: Scratch Cancel General PR193

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: sharipovai-check-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cancel-obsolete-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete PR193 general verification run cancelled by concurrency replacement."

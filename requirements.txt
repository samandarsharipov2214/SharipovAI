# Deploy-locked exact pins for reproducible image builds (F08).
# Keep versions exact (==). Do not loosen to ranges on the deploy path.
fastapi==0.141.1
uvicorn==0.52.4
jinja2==3.1.6
httpx==0.28.1
feedparser==6.0.14
telethon==1.44.0
websockets==17.1
psycopg[binary]==3.3.5
duckdb==1.5.4
prometheus-client==0.26.0
pytest==9.1.1
SQLAlchemy==2.0.52
PyJWT==2.13.0
stripe==15.6.0
email-validator==2.3.0
